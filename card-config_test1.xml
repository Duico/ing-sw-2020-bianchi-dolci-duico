<cardDeck>
    <card>
        <name>DefaultONE</name>
        <moveStrategy>WRONG</moveStrategy>
        <buildStrategy>Default</buildStrategy>
        <winStrategy>Default</winStrategy>
	    <blockStrategy>Default</blockStrategy>
	    <opponentStrategy>Default</opponentStrategy>
    </card>
    <card>
        <name>DefaultTWO</name>
        <moveStrategy>Default</moveStrategy>
        <buildStrategy>WRONG</buildStrategy>
        <winStrategy>Default</winStrategy>
	    <blockStrategy>Default</blockStrategy>
	    <opponentStrategy>Default</opponentStrategy>
    </card>
    <card>
        <name>DefaultTHREE</name>
        <moveStrategy>Default</moveStrategy>
        <buildStrategy>Default</buildStrategy>
        <winStrategy>WRONG</winStrategy>
	    <blockStrategy>Default</blockStrategy>
	    <opponentStrategy>Default</opponentStrategy>
    </card>
</cardDeck>